## Deck Structure

- Revisit Write card identity level: move it to word level (`simplified`) because
  the prompt is not inherently tied to one specific reading. This should be a
  separate model/migration change because it affects generated card counts and
  NoteID rules.
